def fusion(L1, L2):
    """L1 et L2 sont deux tableaux déjà triés dans l'ordre croissant,
    de tailles éventuellement différentes.
    Renvoie un NOUVEAU tableau trié contenant tous leurs éléments.

    ⚠️ Interdit d'utiliser sort() ou sorted() : le tri est déjà fait,
    il suffit d'avancer dans les deux tableaux en même temps."""
    ...

