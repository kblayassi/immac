# Températures relevées par une station météo
temperatures = [12, -3, 8, 15, -7, 4]


def maximum(tableau):
    """Renvoie la plus grande valeur du tableau, sans utiliser max()."""
    ...


def minimum(tableau):
    """Renvoie la plus petite valeur du tableau, sans utiliser min()."""
    ...


def extremum(tableau):
    """Renvoie la liste [maximum, minimum, moyenne] du tableau.
    Appelle obligatoirement les deux fonctions précédentes."""
    ...

