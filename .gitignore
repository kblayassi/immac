# Build MkDocs (regénéré par le CI, cf. .github/workflows/ci.yml)
site/
.cache/

# Dépendances (réinstallables : npm install / pip install)
node_modules/
venv/

# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Évaluations : ce qui ne doit jamais quitter cet ordinateur.
# Le dépôt est PUBLIC — un barème ou un sujet en clair versionné ici serait
# lisible de tous, et le scellement des sujets ne servirait plus à rien.
# Les vraies évaluations vivent dans ce dossier ; garde-le sur la clé USB.
tools/evaluations/prive/
